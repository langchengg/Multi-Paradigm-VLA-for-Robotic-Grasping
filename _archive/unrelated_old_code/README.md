This directory is reserved for legacy code that is no longer part of the clean
VLA grasp decoder benchmark but may still contain useful snippets.

No source files were moved here during the first migration pass because the
legacy demo modules are still referenced by existing compatibility tests and
README history. Generated caches are removed instead of archived.

